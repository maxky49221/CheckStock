
export function createData(product, name, serial, number, price) {
    return {
      product,
      name,
      serial,
      number,
      price, 
      // history: [
      //   { date: '2020-01-05', customerId: '10', amount: 3 },
      //   { date: '2020-01-02', customerId: '20', amount: 1 },
      // ],
    };
  }
  